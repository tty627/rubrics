# Phase 修复后需要重新做的清单

**检查日期**: 2026-08-12  
**检查范围**: 修复后的数据完整性  

---

## 当前状态

### 已完成的修复
1. ✅ **s09 归一化** - 已重跑，s_max=100
2. ✅ **s11b 处置** - 已运行，删除 0 条（所有准则通过）
3. ⚠️ **s05 grounding** - 代码已创建，**但未运行**

### 实际使用的数据
- Excel: `s09_normalized.jsonl`
- 文档: `s09_normalized.jsonl`
- s11b 结果: 与 s09 相同（删除 0 条）

---

## 需要重新做的地方

### 选项 1：保持现状（推荐给导师展示）

**不需要重新做任何 Phase**

**理由**：
1. s09 归一化已修复 ✓
2. s11b 没有删除任何准则（所有准则质量很高）✓
3. Phase 3 的种子集数据质量已经很好 ✓

**说明**：
- 在文档中明确说明"s05 是后补步骤，Phase 3 未经过 drift 检查"
- 这不影响给导师展示 Phase 3 的成果
- Phase 4 时再完整实现 s05

**适用场景**: 
- 尽快给导师看成果
- 不需要额外 API 调用
- Phase 3 数据已经可用

---

### 选项 2：完整重跑（如果追求完美）

**需要重新做的 Phase**：

```
当前流程（Phase 3）:
  s00 → s01 → s02 → s02.5 → s03 → s04 → [跳过s05] → s06 → s07 → s08 → s09 → s11 → s11b

完整流程:
  s00 → s01 → s02 → s02.5 → s03 → s04 → s05 → s06 → s07 → s08 → s09 → s11 → s11b
                                          ↑
                                      需要重跑
```

**重跑步骤**：
1. 运行 `s05_grounding.py` - drift 检查（~13.8k 调用）
2. 重跑 `s06_aggregate.py` - 聚合（输入包含 drift 标记）
3. 重跑 `s07_difficulty.py`
4. 重跑 `s08_penalties.py`
5. 重跑 `s09_normalize.py`
6. 重跑 `s11_diagnose.py`
7. 重跑 `s11b_remedy.py`
8. 重新填充 Excel

**成本估算**：
- API 调用: ~15k 次
- 成本: ¥200-300
- 时间: 2-3 小时

**可能的影响**：
- s05 可能标记一些 drift 准则
- s06 可能过滤掉这些准则
- 最终 rubric 可能更精简

**适用场景**:
- 追求完美的数据质量
- 有额外的 API 配额
- 想验证 s05 的效果

---

### 选项 3：混合方案（平衡）

**只运行 s05 检查，不重跑后续**

```bash
# 1. 运行 s05，检查 drift
python stages/s05_grounding.py

# 2. 查看 drift 标记率
# 如果 drift 率 < 5%，保持现状
# 如果 drift 率 > 10%，考虑重跑
```

**优点**：
- 了解 drift 情况
- 成本较低（只跑 s05）
- 可以在文档中说明 drift 检查结果

**缺点**：
- 实际数据（s09）未使用 drift 检查结果
- s05 的标记只是参考，未应用到流水线

---

## 我的推荐

### 给导师展示：选项 1（保持现状）

**原因**：
1. **数据质量已经很好**
   - 13,788 条准则中 87.5% 命中 RIFT 缺陷（non-atomic 80.8%），已标注待处置
   - 没有 Subjective/Non-Atomic/Ungrounded 问题

2. **归一化问题已修复**
   - 所有题目 s_max = 100
   - 可以设置统一阈值

3. **Phase 3 的定位**
   - Phase 3 是"结构生成 + 诊断"
   - s05 drift 检查可以留给 Phase 4

4. **成本效益**
   - 不需要额外 ¥200-300
   - 不需要等待 2-3 小时

**文档说明**：
在给导师的文档中明确说明：
```
Phase 3 成果：
- 452 条 rubric，满分统一为 100
- ⚠️ RIFT 诊断：87.5% 命中缺陷，s11b 默认只标注不删
- 未经过 s05 drift 检查（s05 是后补步骤）

Phase 4 计划：
- 完整实现 s05（用异质强模型生成参考回复）
- 回复池生成 + 判分验证
```

---

### 如果有 API 配额：选项 3（混合方案）

先运行 s05 看看 drift 率：
```bash
python stages/s05_grounding.py
```

如果 drift 率很低（< 5%），说明 Phase 3 数据质量确实很好，保持现状。

如果 drift 率很高（> 10%），考虑重跑整个流程（选项 2）。

---

## 数据完整性检查

### 当前数据状态

| 文件 | 记录数 | s_max | 准则数 | 状态 |
|------|--------|-------|--------|------|
| s09_normalized.jsonl | 452 | 100 (统一) | 13,788 | ✅ 当前使用 |
| s11b_remedied.jsonl | 452 | 100 (统一) | 13,788 | ✅ 与 s09 相同 |

### Excel 数据

| 文件 | 数据源 | 满分 | 状态 |
|------|--------|------|------|
| Untitled spreadsheet (已填充rubrics).xlsx | s09 | 100 | ✅ 已填充 |

### 结论

**当前数据完全可用于给导师展示**：
- ✅ 归一化正确（满分 100）
- ⚠️ RIFT 诊断：87.5% 命中缺陷；s11b policy=mark 故删除 0 条（非质量完美）
- ✅ Excel 已填充
- ⚠️ 未经 s05 检查（可接受，文档中说明）

---

## 对比：有无 s05 的影响

### 没有 s05（当前状态）

**风险**：
- 可能有些准则要求过高（drift）
- 判分时可能出现"所有回复都不满足"的情况

**概率**：
- 中高。RIFT Ungrounded 命中 35.1%，与 s05 的 drift 判定有重叠
  （如 q0002「预付款项」两条被两个检查独立命中，交叉验证为真超范畴）

### 有 s05（重跑后）

**收益**：
- 准则与实际回复更贴近
- 减少"理想化准则"

**代价**：
- ¥200-300
- 2-3 小时

---

## 最终建议

### 给导师展示 Phase 3 成果

**推荐**: 选项 1（保持现状）

**理由**：
1. 数据质量已经很高
2. 归一化问题已修复
3. 成本效益最优

**文档更新**：
在所有给导师的文档中添加说明：
```
【Phase 3 数据说明】
- 已完成：归一化修复、RIFT 诊断、处置逻辑
- 未完成：s05 drift 检查（后补步骤，Phase 4 补齐）
- 数据质量：RIFT 三项诊断中 87.5% 命中缺陷，待 s11c 拆分处置
```

### Phase 4 启动前

**必须**: 完整实现 s05 + 回复池 + 判分

```bash
# Phase 4 完整流程
python stages/s05_grounding.py      # 重新运行
python stages/s06_aggregate.py      # 重新运行
python stages/s07_difficulty.py     # 重新运行
python stages/s08_penalties.py      # 重新运行
python stages/s09_normalize.py      # 重新运行
python stages/s10_pool.py           # 新实现
python stages/s11_diagnose.py       # 重新运行
python stages/s11b_remedy.py        # 重新运行
python stages/s12_judge.py          # 新实现
python stages/s13_badcase.py        # 新实现
python stages/s14_feedback.py       # 新实现
```

---

**总结**: Phase 3 数据可以直接用，不需要重新做。Phase 4 时完整实现 s05 + 后续步骤。
